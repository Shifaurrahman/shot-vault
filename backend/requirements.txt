# ─── FastAPI ───────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9       # multipart/form-data (file upload)
pydantic==2.7.1
pydantic-settings==2.2.1

# ─── Embedding — Qwen3-VL-Embedding-2B ────────────
torch>=2.2.0
transformers>=4.40.0
accelerate>=0.29.0
Pillow>=10.0.0                 # image loading
sentencepiece                  # tokenizer

# ─── Vector DB — ChromaDB (POC) ───────────────────
chromadb==0.5.0

# ─── Vector DB — Qdrant (prod, optional) ──────────
# qdrant-client==1.9.0

# ─── Storage — S3 (prod, optional) ────────────────
# boto3==1.34.0

# ─── LLM — Claude ─────────────────────────────────
anthropic>=0.28.0

# ─── Utils ────────────────────────────────────────
python-dotenv==1.0.1
aiofiles==23.2.1